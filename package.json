{
  "name": "ecosphere",
  "version": "1.0.0",
  "description": "EcoSphere E-commerce Platform",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && npm run dev\" \"cd frontend && npm start\"",
    "install-all": "npm install && cd frontend && npm install && cd ../server && npm install",
    "frontend": "cd frontend && npm start",
    "server": "cd server && npm run dev"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
